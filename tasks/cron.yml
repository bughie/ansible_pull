---
- name: Add ansible-pull cron job
  ansible.builtin.cron:
    name: "ansible auto-provision"
    user: ansible
    minute: "*/10"
    job: "/usr/bin/ansible-pull -o -U https://github.com/bughie/ansible_pull.git > /dev/null 2>&1"
